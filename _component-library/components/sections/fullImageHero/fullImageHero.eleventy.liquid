<section class="cs-w-full relative">
    {% if content.image.imagePath %}
        <div class="absolute inset-0 -z-10">{%bookshop "generic/image" bind:content.image%}</div>
    {% endif %}
<div class="basecontainer">
<div class="max-w-[600px]">
    
        
    
    <p class=" {% if content.highlightEybrow %} text-{{styles.color_group}}-accentcolor {% endif %} " >{{content.eyebrow}}</p>
    <h1>{{content.headline}}</h1>
    <p>{{content.description}}</p>
    {% if content.buttons %}
        <div>
            {% for button in content.buttons %}
                {%bookshop "generic/button" bind:button%}
            {% endfor %}
            
        </div>
    {% endif %}
</div>
</div>
</section>