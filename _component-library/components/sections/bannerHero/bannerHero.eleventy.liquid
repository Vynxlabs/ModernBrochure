{% capture opacity %}after:bg-opacity-[{{ styles.backgroundOpacity }}%]
{% endcapture %}
{% assign color_group = styles.color_group %}

<div class="c-bannerHero componentcontainer bg-{{styles.color_group}}-backgroundcolor  text-{{styles.color_group}}-textcolor">
    <div class="absolute inset-0 z-10 flex items-center justify-center after:contents[''] after:absolute after:inset-0 after:bg-black after:bg-op {{opacity}} ">
          {% bookshop "generic/image" bind: content.backgroundImage class: "h-[600px] w-screen object-cover " %}
        </div>
    <section class="basecontainer w-full flex flex-col h-full justify-center">
        {% bookshop "generic/heading" bind: content.heading %}
    </section>
</div>