{% capture imageComponentclasses %}
    {% if class %}
        {{class}}
        {% else %}
        w-full h-auto
    {% endif %}

{% endcapture %}

{% if env_bookshop_live %}
    
    <img class="{{imageComponentclasses}}" src="{{ imagePath }}" alt="{{ imageAlt }}">



{% else  %}

{% image imagePath, imageAlt, imageComponentclasses, imageSizes %}

    
{% endif %}