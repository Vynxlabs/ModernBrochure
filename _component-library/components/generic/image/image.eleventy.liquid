{% capture classes %}
    {% if class %}
        {{class}}
        {% else %}
        w-full h-auto
    {% endif %}

{% endcapture %}

{% if env_bookshop_live %}
    
    <img class="{{classes}}" src="{{ imagePath }}" alt="{{ imageAlt }}">



{% else  %}

{% image imagePath, imageAlt, classes, imageSizes %}

    
{% endif %}